# HackAuth

Веб-сервис для **автоматического учёта и оценки продуктовых решений команд**
на хакатонах. Принимает заявки команд (репозиторий, документация, презентация,
скринкаст), выполняет автоматические проверки артефактов, даёт жюри панель
экспертной оценки по настраиваемым весам и формирует итоговый рейтинг.
Дополнительно — модуль проверки алгоритмических задач с песочницей.

## Возможности

- **Роли и доступ:** участник, жюри, организатор. Регистрация создаёт только
  участника; роли жюри назначает организатор (повышением по email или созданием
  аккаунта). Пароли хранятся в виде хэшей.
- **Команды и заявки:** создание команд, вступление по заявке с подтверждением
  капитана, исключение участников, удаление команды. Один участник — одна команда
  на хакатон.
- **Подача решения:** ссылки на репозиторий, документацию, презентацию и видео;
  обновление до дедлайна.
- **Автоматические проверки** (асинхронно через Celery):
  - код — README/лицензия/зависимости/инструкция запуска, LOC, сложность, поиск секретов;
  - документация — обязательные разделы, объём, изображения (PDF/DOCX/Markdown);
  - презентация — количество слайдов, структура (PPTX/PDF);
  - скринкаст — наличие, длительность, технические параметры, саммари.
- **Оценка жюри:** настраиваемые критерии и веса, оценки с комментариями,
  взвешенный итоговый балл, рейтинг команд.
- **Уведомления:** заявки в команду, решения по заявкам, изменения статуса.
- **Алгоритмический модуль:** задачи с тестами и лимитами, запуск решений в
  изолированной песочнице, вердикты OK/WA/TL/ML/RE/CE, история попыток.

## Стек

- **Frontend:** React + TypeScript, Vite, Tailwind CSS, shadcn/ui, React Router.
- **Backend:** FastAPI, SQLAlchemy 2 (async), Alembic, Celery.
- **Хранилище:** PostgreSQL, Redis (брокер очередей и кэш).
- **Инфраструктура:** Docker / docker compose.

## Структура

```
.
├── frontend/                 # React SPA (Vite + Tailwind + shadcn/ui)
├── backend/                  # FastAPI + SQLAlchemy (async) + Celery worker
│   ├── app/
│   │   ├── api/v1/           # REST-эндпоинты
│   │   ├── core/            # конфиг, БД, безопасность, зависимости
│   │   ├── models/         # ORM-модели
│   │   ├── schemas/        # pydantic-схемы
│   │   ├── services/       # авто-проверки и подсчёт баллов
│   │   └── workers/        # задачи Celery
│   ├── alembic/            # миграции
│   ├── scripts/           # seed, smoke
│   ├── docker-compose.yml
│   └── Dockerfile
├── tz.md                    # техническое задание
└── README.md
```

## Запуск

### Backend (Docker)

```bash
cd backend
cp .env.example .env
docker compose up --build
```

Поднимаются сервисы:

- **API** — `http://localhost:8000` (Swagger: `/docs`)
- **PostgreSQL** — `localhost:5432`
- **Redis** — `localhost:6379`
- **Celery worker** — очереди `default`, `checks`, `judge`

Миграции и сидинг демо-данных выполняются автоматически при старте API.

### Frontend (dev)

```bash
cd frontend
npm install
npm run dev          # http://localhost:5173
```

Vite проксирует `/api` на `http://localhost:8000`, поэтому при запущенном
бэкенде дополнительная настройка не нужна. Прод-сборка: `npm run build`.

## Демо-аккаунты

Пароль у всех — `password`:

| Роль        | Логины                                                              |
|-------------|--------------------------------------------------------------------|
| Организатор | `organizer@hackauth.com`                                           |
| Жюри        | `john@jury.com`, `jane@jury.com`, `alex@jury.com`, `reviewer@jury.com` |
| Участники   | `alex@team.com`, `sarah@team.com`, `mike@team.com`                 |

## Полезные команды

```bash
# smoke-тест API (внутри контейнера)
docker compose exec api python -m scripts.smoke

# повторный сидинг
docker compose exec api python -m scripts.seed
```

Контракт REST зафиксирован в [`backend/README.md`](./backend/README.md).
